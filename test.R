#testscript for interactive
#print(system("read -p 'Prompt: ' input; echo $input", intern = TRUE))
#subject= function(){
 # PID=readline(prompt = "Enter the subject number ")
#  subj=return(PID)
#}
#subject()

#system2(subject(), )

args=commandArgs(trailingOnly = TRUE)